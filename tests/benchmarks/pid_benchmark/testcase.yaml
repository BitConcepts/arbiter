tests:
  benchmark.arbiter.pid:
    tags: arbiter benchmark
    platform_allow: native_sim
    harness: console
    extra_args: CONF_FILE=prj.conf
    harness_config:
      regex:
        - "Hand-coded adds: nothing"
